name: Streamlit 自动唤醒

on:
  workflow_dispatch:          # 允许手动按钮触发
  repository_dispatch:        # 允许 Webhook 触发
    types: [service-down-alert]

jobs:
  wakeup:
    runs-on: ubuntu-latest
    
    # 【生产模式已启用】
    # 只有满足以下任一条件才运行：
    # 1. 是手动点击触发的
    # 2. 是 Webhook 触发，且监控名称(monitor_name) 必须等于 'streamlit'
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.client_payload.monitor_name == 'streamlit' }}

    steps:
      - name: 🔍 调试信息 (查看收到的 Payload)
        run: |
          echo "🎉 成功触发！"
          echo "触发来源: ${{ github.event_name }}"
          echo "收到的监控名称: '${{ github.event.client_payload.monitor_name }}'"
          echo "报警状态: ${{ github.event.client_payload.status }}"

      - name: 📥 检出代码
        uses: actions/checkout@v4

      - name: 🌐 设置 Chrome 浏览器
        uses: browser-actions/setup-chrome@v1
        with:
          chrome-version: stable

      - name: 🐍 设置 Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: 📦 安装依赖库
        run: |
          python -m pip install --upgrade pip
          pip install selenium webdriver-manager

      - name: 🚀 执行唤醒
        env:
          STREAMLIT_APP_URL: ${{ secrets.STREAMLIT_APP_URL }}
        run: |
          if [ -f "keep/streamlit-keep.py" ]; then
            python keep/streamlit-keep.py
          else
            echo "❌ 错误: 找不到 keep/streamlit-keep.py 文件"
            exit 1
          fi
